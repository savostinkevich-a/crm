import React, {FC} from 'react';
import {Layout} from "antd";

const SiteFooter: FC = () => {
    return (
        <Layout.Footer>
            
        </Layout.Footer>
    );
};

export default SiteFooter;