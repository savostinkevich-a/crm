import React, {FC} from 'react';
import {Layout} from "antd";

const SiteHeader: FC = () => {
    return (
        <Layout.Header>
            
        </Layout.Header>
    );
};

export default SiteHeader;